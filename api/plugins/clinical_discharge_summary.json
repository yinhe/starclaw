{
  "name": "clinical_discharge_summary",
  "description": "Transform discharge note text into a structured discharge summary with diagnoses, medications, lab items, and discharge advice.",
  "version": "1.0.0",
  "author": "StarClaw Clinical Note Clinic",
  "parameters": {
    "type": "object",
    "properties": {
      "action": { "type": "string", "const": "discharge_summary" },
      "note_text": { "type": "string", "description": "Discharge note or hospitalization summary text" }
    },
    "required": ["action", "note_text"]
  },
  "handler": "clinical_note_clinic",
  "metadata": { "category": "medical", "timeout": 10 }
}
