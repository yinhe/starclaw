{
  "name": "lab_report_parse",
  "description": "Parse pasted lab report text into structured pediatric lab metrics, suggest matching panel types, and prepare downstream tool payloads.",
  "version": "1.0.0",
  "author": "StarClaw Lab Clinic",
  "parameters": {
    "type": "object",
    "properties": {
      "action": { "type": "string", "const": "parse_report" },
      "report_text": { "type": "string", "description": "Raw pasted lab report text" },
      "panel_hint": { "type": "string", "description": "Optional panel hint: cbc / iron / thyroid / vitamin / metabolic" },
      "age_years": { "type": "number", "description": "Optional patient age in years to help downstream screening thresholds" }
    },
    "required": ["action", "report_text"]
  },
  "handler": "lab_clinic",
  "metadata": { "category": "medical", "timeout": 12 }
}
