{
  "name": "clinical_note_parse",
  "description": "Parse outpatient notes or discharge summaries into structured sections, diagnoses, medications, lab mentions, follow-up items, and risk reminders.",
  "version": "1.0.0",
  "author": "StarClaw Clinical Note Clinic",
  "parameters": {
    "type": "object",
    "properties": {
      "action": { "type": "string", "const": "parse_note" },
      "note_text": { "type": "string", "description": "Raw outpatient note or discharge summary text" }
    },
    "required": ["action", "note_text"]
  },
  "handler": "clinical_note_clinic",
  "metadata": { "category": "medical", "timeout": 10 }
}
