# ⚠️ DEPRECATED — This config is now consolidated into queen/deploy/nginx-queen.conf
# Remove symlink on server: sudo rm -f /etc/nginx/sites-enabled/nydus
#
# Keeping this file for reference only. Do NOT deploy separately.
# Having both this AND nginx-queen.conf active causes DUPLICATE server blocks
# which breaks SSL (nginx picks the wrong block for SNI matching).
