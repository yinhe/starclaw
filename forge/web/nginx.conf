server {
    listen 3099;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        proxy_pass http://forge-api:8099;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /health {
        proxy_pass http://forge-api:8099;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
