{
  "name": "growth_education",
  "description": "Push health education content to patient/guardian — diet, exercise, sleep, puberty knowledge. Persists education record for audit trail.",
  "version": "1.0.0",
  "author": "StarClaw Growth Clinic",
  "parameters": {
    "type": "object",
    "properties": {
      "action": { "type": "string", "const": "education_push" },
      "patient_id": { "type": "string" },
      "category": { "type": "string", "description": "diet / exercise / sleep / puberty / general" },
      "alert_level": { "type": "string", "description": "normal / mild / moderate / severe — adjusts content tone" }
    },
    "required": ["action"]
  },
  "handler": "growth_clinic",
  "metadata": { "category": "medical", "timeout": 10 }
}
