# NATS Server Configuration
port: 4222
server_name: pheromone-nats

# JetStream
jetstream {
  store_dir: /data/nats
}

# HTTP monitoring
http_port: 8222

# WebSocket listener (for cross-region clients behind restrictive firewalls)
websocket {
  port: 8880
  no_tls: true
}
